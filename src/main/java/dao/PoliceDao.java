package dao;

import javafx.application.Application;
import javafx.stage.Stage;
import models.Police;
import org.sql2o.Query;

import java.util.List;

public class PoliceDao extends Application {

    private static class Police;

    private static class Police;

    private class Police;

    public static void main(String[] args) {
        launch(args);
    }

    @Override
    public void start(Stage primaryStage) {
//get the user to Database the police
        void add(Police );


        //get all users database
        List<Police> getAll();

    }

    public Query createQuery(String s, boolean b) {
    }
}

